/// CourtStatsCore — event-sourced basketball stats processing library.
/// Parses CS: markers from FCPXML, computes projections, generates overlays.
public enum CourtStatsCore {
    public static let version = "0.1.0"
}
